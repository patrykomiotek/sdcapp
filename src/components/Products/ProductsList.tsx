import { useEffect, useState } from "react";

export const ProductsList = () => {
  const products = useState();

  useEffect(() => {
    // API request
    // set state
  }, []);

  return (
    <div>

    </div>
  );
}
